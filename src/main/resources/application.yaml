server:
  port: 8100
  servlet:
    context-path: /api
spring:
  application:
    name: zt-oj
    #数据库配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ztoj
    username: root
    password: root
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: false
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      logic-delete-field: isDelete
      logic-delete-value: 1 #逻辑删除值
      logic-not-delete-value: 0 #逻辑未删除值
#接口文档配置
knife4j:
  enable: true
  openapi:
    title: 竞赛系统
    description: “你好，我是墨城有珠"
    email: 2837198145@qq.com
    version: v1.0
    group:
      default:
        api-rule: package
        api-rule-resources:
          - com.zt.oj.controller